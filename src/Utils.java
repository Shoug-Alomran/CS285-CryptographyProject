public class Utils {
    // Task: Create a static SecureRandom object for use across classes

    // Task: Method getRandom(upperLimit)
    // - Return random BigInteger in range [1, upperLimit - 1]

    // Task: Method printLine(label, value)
    // - Format console output for screenshots (e.g., "User A public key: 8")

    // Task: Method normalizeKeyBytes(BigInteger key)
    // - Remove leading zero byte if present in key.toByteArray()
}
