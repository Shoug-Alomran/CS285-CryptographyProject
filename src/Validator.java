public class Validator {
    // Task: Method isPrime(q) → return true if q is prime using isProbablePrime()

    // Task: Method isValidMessage(message) → return true if message length > 20

    // Task: Method isAlphaInRange(alpha, q) → ensure 1 < alpha < q

    // Task: Optional method to print validation errors with clear messages
}
